export enum ConvertTypes {
  TXT = 'TXT',
  HTML = 'HTML',
  PDF = 'PDF',
  IMAGE = 'IMAGE',
  AUDIO = 'AUDIO',
}

export const enum ImageTypes {
  JPG = 'JPG',
}

export const enum AudioTypes {
  MP3 = 'MP3',
}
